(ns sixsq.slipstream.pricing.service.server-test
  (:require [clojure.test :refer :all]
            [sixsq.slipstream.pricing.service.server :refer :all]))

(deftest a-test
  (testing "FIXME, I pass."
    (is (= 0 0))))
